{
  "name": "aws-node-http-api",
  "version": "1.0.0",
  "description": "Klippr video functions",
  "main": "handler.js",
  "scripts": {
    "start": "AWS_PROFILE=offline serverless offline",
    "deploy": "serverless deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "centra": "^2.7.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.1430.0",
    "serverless-jetpack": "^0.11.2",
    "serverless-offline": "^14.1.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
